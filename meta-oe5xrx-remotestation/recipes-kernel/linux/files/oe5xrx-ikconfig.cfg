# Build the running kernel config into the image (/proc/config.gz) so config
# parity with Raspberry Pi OS can be verified on-target and USB issues debugged
# without a rebuild. Built-in (=y) so no `modprobe configs` is needed.
CONFIG_IKCONFIG=y
CONFIG_IKCONFIG_PROC=y
