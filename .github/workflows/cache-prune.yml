name: Prune shared cache

on:
  schedule:
    # Nightly at 03:43 UTC — dry-run by default (safe).
    # To enable real deletion set dry_run=0 via workflow_dispatch.
    - cron: '43 3 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'List only (1) or delete (0)'
        default: '1'

# Least privilege: prune only reads the repo + mounts the box. No writes.
permissions:
  contents: read

jobs:
  prune:
    name: Prune sstate + downloads
    runs-on: ubuntu-latest
    env:
      BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}
      BWS_SERVER_URL: ${{ secrets.BWS_SERVER_URL }}

    steps:
      - uses: actions/checkout@v6

      - name: Fetch Storage Box creds from Bitwarden
        run: |
          set -euo pipefail
          # Install bws (pinned + sha256, mirroring servers/cloud-init). jq is
          # preinstalled on ubuntu-latest.
          VER=0.5.0
          SHA=b9296341549d9ba6922da6692b24c4d81d14dc3992597d5a777692aee73b10b2
          curl -fsSL -o /tmp/bws.zip "https://github.com/bitwarden/sdk-sm/releases/download/bws-v${VER}/bws-x86_64-unknown-linux-gnu-${VER}.zip"
          echo "${SHA}  /tmp/bws.zip" | sha256sum -c -
          mkdir -p "$HOME/.local/bin"; unzip -o /tmp/bws.zip -d "$HOME/.local/bin"
          export PATH="$HOME/.local/bin:$PATH"
          export BWS_SERVER_URL="${BWS_SERVER_URL:-https://vault.bitwarden.eu}"
          PROJECTS_JSON=$(bws project list -o json) || { echo "::error::bws project list failed (check BWS_ACCESS_TOKEN / network)"; exit 1; }
          PROJECT_ID=$(jq -r '.[] | select(.name=="oe5xrx-yocto-cache") | .id' <<<"${PROJECTS_JSON}")
          [ -n "${PROJECT_ID}" ] || { echo "::error::Bitwarden project oe5xrx-yocto-cache not found (check read-access)"; exit 1; }
          SECRETS=$(bws secret list -p "${PROJECT_ID}" -o json) || { echo "::error::bws secret list failed for project oe5xrx-yocto-cache"; exit 1; }
          BOX_HOST=$(jq -r '.[]|select(.key=="STORAGE_BOX_HOST")|.value' <<<"${SECRETS}")
          BOX_USER=$(jq -r '.[]|select(.key=="STORAGE_BOX_USER")|.value' <<<"${SECRETS}")
          if [ -z "${BOX_HOST}" ] || [ -z "${BOX_USER}" ]; then
            echo "::error::STORAGE_BOX_HOST/USER missing in oe5xrx-yocto-cache"; exit 1
          fi
          if [[ "${BOX_HOST}" == *$'\n'* || "${BOX_USER}" == *$'\n'* ]]; then
            echo "::error::STORAGE_BOX_HOST/USER must be single-line"; exit 1
          fi
          echo "STORAGE_BOX_HOST=${BOX_HOST}" >> "${GITHUB_ENV}"
          echo "STORAGE_BOX_USER=${BOX_USER}" >> "${GITHUB_ENV}"
          # Private key is multi-line (PEM): write straight to a mode-600 file,
          # NEVER to $GITHUB_ENV or a :: command (masking only covers the first
          # line → leak/injection). $RUNNER_TEMP is auto-cleaned at job end.
          KEYFILE="${RUNNER_TEMP}/storagebox_key"
          ( umask 077; jq -r '.[]|select(.key=="STORAGE_BOX_SSH_PRIVKEY")|.value' <<<"${SECRETS}" > "${KEYFILE}" )
          [ -s "${KEYFILE}" ] || { echo "::error::STORAGE_BOX_SSH_PRIVKEY missing/empty in oe5xrx-yocto-cache"; exit 1; }
          echo "STORAGE_BOX_KEYFILE=${KEYFILE}" >> "${GITHUB_ENV}"

      - name: Install sshfs
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends sshfs

      - name: Mount Storage Box on runner
        run: |
          set -euo pipefail
          grep -q '^user_allow_other' /etc/fuse.conf || echo 'user_allow_other' | sudo tee -a /etc/fuse.conf
          mkdir -p /mnt/yocto-shared
          sudo sshfs -p 23 \
            -o IdentityFile="${STORAGE_BOX_KEYFILE}",StrictHostKeyChecking=accept-new \
            -o allow_other,reconnect,ServerAliveInterval=15,ServerAliveCountMax=3 \
            "${STORAGE_BOX_USER}@${STORAGE_BOX_HOST}:" /mnt/yocto-shared
          mountpoint -q /mnt/yocto-shared
          echo "== mount ==" && mount | grep yocto-shared || true
          df -h /mnt/yocto-shared

      - name: Checkout openembedded-core (for sstate-cache-management.py)
        # Pin to the same branch the builds use (per the lockfiles), so the prune
        # tool doesn't drift to an arbitrary upstream HEAD.
        run: git clone --depth 1 --branch wrynose --single-branch https://git.openembedded.org/openembedded-core

      - name: Prune cache
        env:
          DRY_RUN: ${{ github.event.inputs.dry_run || '1' }}
          OE_CORE: openembedded-core
        run: |
          # Allowlist: reject anything that isn't exactly '0' or '1'.
          case "${DRY_RUN}" in
            0|1) ;;
            *) echo "::error::dry_run must be '0' or '1', got '${DRY_RUN}'"; exit 1 ;;
          esac
          scripts/cache-prune.sh
